|module.run(['$templateCache', ($templateCache) ->
|  $templateCache.put('contrib/iframe-show.html', '''
div.wrapper
    tg-project-menu
    section.main.iframe
      h1 
        a(href="{{iframe.url}}") {{iframe.title}}
      div.extra_html(ng-show="iframe.html" ng-bind-html="trustHtml(iframe.html)")
      iframe(src="{{trustSrc(iframe.url)}}", name="{{iframe.slug}}", style="width:100%; min-height:calc(100vh - 40px);")
|''')]);
